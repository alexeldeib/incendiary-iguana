name: build + test
on: [push]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: build
        uses: ./.github/actions/build
        with:
          args: ["make", "manager"]